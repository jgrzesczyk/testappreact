import { Card } from '@mui/material';
import styled from 'styled-components';

export const UsersReposStyled = styled(Card)`
  max-width: 800px;
  margin: 0 auto;
  width: 100%;
`;

export const SpinnerContainer = styled.div`
  display: flex;
  justify-content: center;
`;
